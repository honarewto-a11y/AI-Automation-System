module.exports = function existenceEngine({ diagnostic, rootCause }) {
    // نسخه‌ی پایه: فقط سوال‌های وجودی تمدنی تولید می‌کند
    const questions = [];

    questions.push("سؤال وجودی: اگر این نوع خطا تکرار شود، آیا معماری فعلی تمدن کافی است؟");
    questions.push("سؤال وجودی: آیا باید نسل بعدی NoraCivilis با معماری متفاوت طراحی شود؟");
    questions.push("سؤال وجودی: آیا قوانین فعلی تمدن برای تاب‌آوری بلندمدت کافی هستند؟");

    return questions;
};
