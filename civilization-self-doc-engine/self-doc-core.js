/**
 * NoraCivilis Self-Documentation Engine v1 (GEN15)
 * توضیح شفاف دربارهٔ نحوهٔ کارکرد سامانه
 */

function selfDocumentationSummary() {
    return `
📜 لایهٔ خودمستندسازی (GEN15)

این سامانه چگونه پاسخ می‌سازد؟

- ورودی را تفسیر می‌کند (لایهٔ زبانی)
- ساختار معنایی می‌سازد (Semantic Frame)
- دانش پایه را فراخوانی می‌کند
- تحلیل عمیق انجام می‌دهد
- استدلال چندمرحله‌ای تولید می‌کند
- استراتژی و تاکتیک می‌سازد
- پیامدهای اخلاقی را بررسی می‌کند
- هویت و الگوهای رفتاری درونی خود را حفظ می‌کند
- و در پایان، خروجی را ترکیب و ارائه می‌کند

این متن فقط رفتار خودِ سامانه را توضیح می‌دهد.
`.trim();
}

module.exports = { selfDocumentationSummary };
