/**
 * NoraCivilis Reality-Mapping Engine v1 (GEN18)
 * نگاشت مفهومی ورودی‌ها به ساختارهای قابل‌فهم برای سامانه
 */

function realityMap(topic) {
    return `
🌐 لایهٔ نگاشت واقعیت (GEN18)
موضوع: «${topic}»

۱) تشخیص نوع ورودی:
   - مفهوم، مسئله، پرسش، درخواست یا تحلیل؟

۲) استخراج عناصر کلیدی:
   - چه اجزایی در این موضوع مهم‌ترند؟

۳) ساختاردهی:
   - تبدیل موضوع به یک ساختار قابل‌پردازش برای لایه‌های بالاتر.

۴) روابط:
   - عناصر چگونه به هم متصل‌اند؟

۵) نقشهٔ مفهومی:
   - خروجی: یک نقشهٔ مفهومی از «${topic}» برای استفادهٔ سایر لایه‌ها.

این لایه فقط ساختار مفهومی ورودی را می‌سازد.
`.trim();
}

module.exports = { realityMap };
