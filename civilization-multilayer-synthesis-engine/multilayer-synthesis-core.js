/**
 * NoraCivilis Multi-Layer Synthesis Engine v1 (GEN19)
 * ترکیب خروجی لایه‌های مختلف به یک درک منسجم
 */

function multilayerSynthesis(summaryPieces) {
    return `
🧩 لایهٔ ترکیب چندلایه‌ای (GEN19)

این لایه چه می‌کند؟

- قطعات مختلف فهم (دانش، استدلال، اخلاق، تاکتیک، استراتژی و ...) را کنار هم می‌گذارد.
- تناقض‌های آشکار را شناسایی می‌کند.
- سعی می‌کند یک تصویر منسجم از موضوع بسازد.
- نقاط ابهام را مشخص می‌کند.
- در نهایت، یک جمع‌بندی چندلایه‌ای ارائه می‌دهد.

خلاصهٔ لایه‌ها:
${summaryPieces}

این لایه فقط خروجی لایه‌های دیگر را ترکیب و منسجم می‌کند.
`.trim();
}

module.exports = { multilayerSynthesis };
